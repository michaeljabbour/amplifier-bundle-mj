"""Job implementations package.

Concrete job classes live here.  Import them explicitly where needed;
this __init__.py intentionally does not re-export them to avoid accidentally
loading CleanupJob (T2) during tests.
"""
